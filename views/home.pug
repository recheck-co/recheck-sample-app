extends layout

block content
  div(class="jumbotron")
    div(class="container")
      h1= title
      p You are now logged in!
      form(action='/logout' method='post')
        button(class='rounded py-1 px-2 btn btn-secondary' type='submit' value='Logout') Logout
